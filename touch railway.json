{
  // Railway の設定ファイル
  "services": {
    "mirai-heko-system": {      // ← Service カードの名前と合わせる
      "serviceType": "worker",  // ここがポイント！
      // 任意：カスタム開始コマンド
      "startCommand": "python learner_main.py",
      // 任意：ルートディレクトリを指定する場合
      "rootDirectory": "./"
    }
  }
}
